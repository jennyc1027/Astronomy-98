import numpy as np 

def sorting(matrix):

    sort1 = np.argsort(matrix, axis=0)
    sort2 = matrix[sort1, np.arange(matrix.shape[1])]

    return sort2
np.random.seed(12345)

a = np.random.randint(1, 50, (4,5))
print("Original matrix:")
print(a)

sort2 = sorting(a)
print(sort2)