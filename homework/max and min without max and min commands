list = eval(input("input your choice of a list of numbers"))

var1 = list[0] 
var2 = list[0] 

for num in list:
    if num > var1:
        var1 = num
    if num < var2:
        var2 = num

print("max:", var1) 
print("min:", var2) 

#searched online for how to sequence the num and var1, var2 
#went onto stack overflow to find eval(input) command rather than what I did in the other problems with int or float