def digrt(n):
    var1 = sum([int(num) for num in str(n)])
    if len(str(sums)) >= 2:
        sums = digrt(var1)
    return var1
    
#most of code inspired by github from searching how to use len and the if loop